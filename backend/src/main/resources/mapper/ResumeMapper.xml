<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.green.backend.repository.dao.ResumeDao">
    <insert id="insertResumeMain">
        insert into tbl_resume value
        (null,'#{resume.resumeTitle}','#{resume.resumeArea}',
        '#{resume.resumeMainYn}','#{resume.resumeMemo}','#{resume.instId}',now())
    </insert>
</mapper>